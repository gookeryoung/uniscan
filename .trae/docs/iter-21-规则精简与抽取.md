# iter-21：rule-12 精简整合与项目特定内容抽取

## 本轮目标

将 `rule-12-pyqt-standards.md` 中项目特定的界面布局规范抽取到 `.trae/skills/`，并对 rule-12 通用内容精简整合，保持语义一致。rule-12 回归「通用 PySide2/PySide6 编码规范」定位，项目特定约束由 skills 承载。

## 改动文件清单

### 规则文件（1 文件）

- `.trae/rules/rule-12-pyqt-standards.md`（精简，325 行 → 220 行）：
  - **删除**：「界面布局（GitHub Desktop 风格）」整章（原第 85-172 行，含 ASCII 示意图、5 区职责、9 个子节）。
  - **删除**：「布局」「样式（QSS）」章节末尾指向界面布局章节的两处交叉引用。
  - **新增**：开头补充引导语「项目特定的界面布局规范见 `.trae/skills/` 下对应文档」。
  - **精简整合**（保持语义）：
    - 兼容性章节：「版本与依赖」合并为 1 条；「双兼容编码模式」压缩解释文字。
    - 模块与入口：4 条合并为 3 条（惰性导入并入第 3 条）。
    - 信号槽：代码示例从完整 import + class 简化为类片段；合并重复连接与 disconnect 条目。
    - 布局：5 条合并为 3 条（嵌套布局与伸缩比合并；尺寸策略与响应式合并）。
    - 资源系统：4 条 + 编译说明合并为 3 条。
    - 事件循环：4 条合并，压缩解释。
    - 打包：合并 `--noconsole` 与 `--hidden-import` 说明条目。
    - 国际化：合并 `.ts` 提取编译与运行时加载条目。
    - 其余章节保持条目数，仅压缩措辞。

### skills 文件（1 文件，新增）

- `.trae/skills/uniscan-gui-layout.md`：承载 uniscan 项目特定的 GitHub Desktop 风格界面布局规范，含：
  - 5 区布局结构 ASCII 示意图
  - 5 区职责（含 uniscan 业务映射：扫描模式/规则/命中/导出等）
  - 8 个子节：菜单栏、主操作区、列表区、底部操作区、详情操作栏、详情主体、分割器与伸缩、样式约束
  - 开头说明「通用编码规范见 rule-12」

## 关键决策与依据

1. **rule-12 回归通用定位**：rule-12 是 `project_type=gui` 的通用规则，不应锚定特定设计风格（GitHub Desktop）或特定业务（扫描/命中）。项目特定约束移至 skills，使 rule-12 可复用于其他 PyQt 项目。
2. **整体移走而非拆分**：「界面布局（GitHub Desktop 风格）」章节整体移到 skills，未拆分「通用布局原则」与「业务映射」。理由：GitHub Desktop 风格本身即项目特定选择，且 rule-12 既有「布局」章节已含通用布局原则（QVBoxLayout/QSplitter/stretch），无需补充。
3. **skill 文件独立而非追加**：新建 `uniscan-gui-layout.md` 而非追加到既有 `uniscan-development.md`。理由：主题独立（界面布局 vs 通用开发技能），内容较多（5 区 + 业务映射 + 配色），独立文件便于查找维护。
4. **精简策略：合并冗余 + 压缩解释，保持约束语义**：不删减约束条目，仅合并表述相近的条目、去除解释性文字。如「模块与入口」4 条合并为 3 条（惰性导入并入业务逻辑条目），「布局」5 条合并为 3 条（嵌套布局与伸缩比合并）。
5. **开头引导语**：rule-12 开头加一句指向 skills 的引导，避免读者误以为界面布局规范缺失。

## 验证结果

- 章节结构：`grep '^## ' rule-12` 确认 16 个二级章节（原 17 个减去「界面布局」），通用规则完整。
- 行数：325 → 220 行，精简 32%。
- 语义保持：逐章对照，所有约束条目均保留（部分合并表述），无约束丢失。
  - 兼容性：版本/依赖/API 差异/双兼容模式 全部保留。
  - 信号槽：新式语法/类属性/方法引用/重复连接/跨线程 全部保留。
  - QThread：两种模式/线程安全/退出/清理/锁 全部保留。
  - 测试：qapp/qtbot/waitSignal/gui 标记/offscreen 全部保留。
  - 打包：PyInstaller 命令/`_MEIPASS`/Nuitka 全部保留。
  - 其余章节条目均保留。
- skill 文件完整性：`uniscan-gui-layout.md` 含原界面布局章节全部内容 + 项目背景说明，约束无丢失。
- Markdown 格式：代码块闭合正确，表格格式正确，标题层级一致。

## 遗留事项

- GUI 代码（main_window.py）仍未按 skill 中的 GitHub Desktop 布局重构，作为后续迭代依据。
- req-03 需求项仍为 `[ ]`，待 GUI 重构落地后标记完成。
- coverage 与 ruff UP006/UP045 既有技术债：与本轮无关。
